# PenPard Environment Configuration
# Copy this file to .env and customize values

# Log timezone: agent logs use this for timestamps (default: server TZ). Set to your local timezone if logs show wrong time (e.g. UTC).
# LOG_TZ=Europe/Istanbul

# JWT Secret (REQUIRED for persistent sessions - if not set, a random key is generated per restart)
JWT_SECRET=change-this-to-a-random-secret-key

# CORS Origins (comma-separated, defaults to localhost:3000 if not set)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# Database
DATABASE_PATH=/app/data/penpard.db

# Burp Suite Professional Configuration
# MCP Extension (port 9876 for SSE)
# Use localhost:9876 for npm, host.docker.internal:9876 for Docker
BURP_MCP_URL=http://localhost:9876
# REST API (port 8082)
# Use localhost:8082 for npm, host.docker.internal:8082 for Docker
BURP_API_URL=http://localhost:8082
BURP_PATH=/path/to/BurpSuitePro

# MobSF Configuration
# Use localhost:8000 for npm, host.docker.internal:8000 for Docker
MOBSF_API_URL=http://localhost:8000
MOBSF_API_KEY=your-mobsf-api-key

# LLM API Keys (configure the providers you use)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=...
# DEEPSEEK_API_KEY=...

# Email Configuration (optional - uses console logging if not set)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@localhost
